# WaraNode Configuration - Environment Template

# 1. Database (SQLite)
# Default is a local dev.db file inside the prisma folder.
DATABASE_URL=file:./dev.db

# 2. Blockchain (Ethereum Sepolia)
# Use your own Infura/Alchemy key to avoid public RPC rate limits.
RPC_URL=https://sepolia.infura.io/v3/2260b5ca05c2455a8a5047f1f7e233fc

# 3. Metadata (TMDB)
# Optional but recommended for movie/series metadata.
# Get one at https://www.themoviedb.org/documentation/api
TMDB_API_KEY=

# 4. Networking
PORT=21746

# 5. Security (Dashboard Centralized Control)
# true: Only accessible via localhost (Maximum Security)
# false: Allows remote connection via admin key
LOCAL_ONLY=true
